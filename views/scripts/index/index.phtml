<?php
/**
 * Docs landing (stub). Renders inside the theme's public layout — a theme can override this
 * view. The real docs browser (article tree + search + content pages) replaces this.
 */
?>
<div class="container py-5" style="max-width:820px;">
  <header class="mb-4">
    <div class="small text-uppercase text-body-secondary mb-1" style="letter-spacing:.09em;font-weight:600;">Documentation</div>
    <h1 class="display-6 fw-bold mb-2">Docs</h1>
    <p class="text-body-secondary">This is the <code>docs</code> module — installed from
       <a href="https://github.com/WebTigers/tiger-docs" class="text-decoration-none">WebTigers/tiger-docs</a>
       via the Tiger Module Installer.</p>
  </header>

  <div class="card">
    <div class="card-body">
      <p class="mb-0 text-body-secondary">
        <i class="fa-regular fa-file-lines me-2"></i>
        The documentation browser (article tree, search, and content pages) is the next build.
        This stub proves the install → auto-discover → route pipeline end to end.
      </p>
    </div>
  </div>
</div>
